/** @type {import('next').NextConfig} */
// Dynamic Next.js configuration for Netlify deployment
// We omit the `output` property so Netlify can generate both static and dynamic routes.
// See https://docs.netlify.com/build/frameworks/framework-setup-guides/nextjs/overview/ for details.
const nextConfig = {
  // Add any Next.js configuration options here.
};
module.exports = nextConfig;
